<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">Memento Mori</a>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <%if @current_user %>
        <li class="nav-item dropdown">
          <div class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= @current_user.first_name %>
          </div>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <div class="dropdown-item"> <%= link_to 'Dashboard', dashboard_path(@current_user) %> </div>
            <div class="dropdown-item"> <%= link_to 'Settings', settings_path %> </div>
            <div class="dropdown-item"> <%= link_to 'Sign Out', sign_out_path, method: :delete %> </div>
          </div>
      <%else%>
        <li class="nav-item active">
           <div class="nav-link"> <%= link_to "Sign In", sign_in_path %> </div>
        </li>
        <li class="nav-item active">
          <div class="nav-link"> <%= link_to "Register", sign_up_path %> </div>
        </li>
      <%end%>
    </ul>
  </div>
</nav>

